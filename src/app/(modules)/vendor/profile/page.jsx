import ProfileForm from "./ProfileForm";

export const metadata = {
    title: 'Vendor Guide | Profile'
  }

const Page = () => {
   
    return (
        <ProfileForm/>
    );
};

export default Page;